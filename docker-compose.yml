version: "3.9"
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: api
    env_file: [ .env ]
    ports: [ "8000:8000" ]

  worker:
    build:
      context: .
      dockerfile: Dockerfile
      target: celery
    env_file: [ .env ]
